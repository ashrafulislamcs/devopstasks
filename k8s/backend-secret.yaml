# EXAMPLE ONLY - do not commit real values.
# In real use this is created via `kubectl create secret` from a value
# pulled out of AWS Secrets Manager / Azure Key Vault at deploy time,
# or synced automatically using the External Secrets Operator / CSI driver.
apiVersion: v1
kind: Secret
metadata:
  name: backend-db-secret
  namespace: app
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_ME_AT_DEPLOY_TIME"
